#include <stdio.h>

int main() {

    char nome;
    double salario,vendas,x;

  scanf("%s",&nome);
  scanf("%lf",&salario);
  scanf("%lf",&vendas);

  x = salario + (15*vendas)/100;

   printf("TOTAL = R$ %.2f\n",x);

    return 0;
}

