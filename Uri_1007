#include<stdio.h>
#include<stdio.h>
main(){
    int A,B,C,D,DIFERENCA;

    scanf("%d",&A);
    scanf("%d",&B);
    scanf("%d",&C);
    scanf("%d",&D);

    DIFERENCA = (A * B - C * D);

    printf("DIFERENCA = %d",DIFERENCA);

    system("pause");
    return 0;
}
